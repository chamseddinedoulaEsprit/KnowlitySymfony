<form method="post" action="{{ path('app_user_quiz_selection_delete', {'id': user_quiz_selection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_quiz_selection.id) }}">
    <button class="btn">Delete</button>
</form>
