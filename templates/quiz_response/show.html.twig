{% extends 'base2.html.twig' %}

{% block title %}QuizResponse{% endblock %}

{% block body %}
    <h1>QuizResponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quiz_response.id }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ quiz_response.texte }}</td>
            </tr>
            <tr>
                <th>EstCorrecte</th>
                <td>{{ quiz_response.estCorrecte ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quiz_response_index') }}">back to list</a>

    <a href="{{ path('app_quiz_response_edit', {'id': quiz_response.id}) }}">edit</a>

    {{ include('quiz_response/_delete_form.html.twig') }}
{% endblock %}
