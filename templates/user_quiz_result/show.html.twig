{% extends 'base.html.twig' %}

{% block title %}UserQuizResult{% endblock %}

{% block body %}
    <h1>UserQuizResult</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_quiz_result.id }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ user_quiz_result.score }}</td>
            </tr>
            <tr>
                <th>SoumisLe</th>
                <td>{{ user_quiz_result.soumisLe ? user_quiz_result.soumisLe|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Feedback</th>
                <td>{{ user_quiz_result.feedback }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ user_quiz_result.user }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_quiz_result_index') }}">back to list</a>

    <a href="{{ path('app_user_quiz_result_edit', {'id': user_quiz_result.id}) }}">edit</a>

    {{ include('user_quiz_result/_delete_form.html.twig') }}
{% endblock %}
