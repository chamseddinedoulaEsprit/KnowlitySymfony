{% extends 'base2.html.twig' %}

{% block title %}New Quiz{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="quiz-title">Create new Quiz</h1>
        <div class="form-container" style="float: left; width: 48%; margin-right: 2%;">
            <div class="card">
                <div class="card-body">
                    {{ include('quiz/_form.html.twig') }}
                </div>
            </div>
            <a href="{{ path('app_quiz_index') }}" class="btn btn-secondary mt-3">Back to list</a>
        </div>
    </div>
{% endblock %}